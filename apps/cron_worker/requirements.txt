# Minimal runtime for meal-rise Vercel Python worker (see pyproject.toml for full repo deps).
# Pinned to exact versions resolved by uv.lock; keep in sync when the lock updates.
pandas==3.0.1
numpy==2.4.3
psycopg2-binary==2.9.12
pydexcom==0.5.1
python-dotenv==1.2.2
pyyaml==6.0.3
requests==2.32.5
